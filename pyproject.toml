[tool.black]
line-length = 100
target-version = ["py39"]

[tool.ruff]
line-length = 100
target-version = "py39"
exclude = ["docs", "cdk.out", "dist", "node_modules", "build"]

[tool.ruff.lint]
select = ["E", "F", "I", "B", "UP"]
